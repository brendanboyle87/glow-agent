This project is Jericho-first, not emulator-agnostic first.
Use Jericho get_state/set_state as a first-class capability, not a backend detail.
Do not replace native snapshot restore with generic replay unless used as fallback.
