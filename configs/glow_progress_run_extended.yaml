extends: glow_progress_run.yaml

runtime:
  docker_notes:
    - "Longer progress-seeking run after stale-revisit classification was fixed."
    - "Same live full path, larger budget, fresh artifacts."

paths:
  artifacts_dir: artifacts/progress/extended_progress_run_live_qwen9b
  trajectory_dir: artifacts/progress/extended_progress_run_live_qwen9b/trajectories
  archive_dir: artifacts/progress/extended_progress_run_live_qwen9b/archive
  log_dir: artifacts/progress/extended_progress_run_live_qwen9b/logs
  summary_dir: artifacts/progress/extended_progress_run_live_qwen9b/summaries
  metrics_dir: artifacts/progress/extended_progress_run_live_qwen9b/metrics

logging:
  run_log_filename: progress_run_extended.log

policy:
  frontier_max_size: 24
  archive_top_k: 12

experiment:
  max_steps: 256
  max_replay_attempts: 48
  glow_frontier_size: 10
